name: 信号响应测试

on:
  # 接收来自 Worker 的 repository_dispatch 信号
  repository_dispatch:
    types: [org-webhook]
  # 支持手动在网页点一下，用于对比测试
  workflow_dispatch:

jobs:
  ping-test:
    runs-on: ubuntu-latest
    steps:
      - name: 报告状态
        run: |
          echo "🚀 成功接收到来自 Worker 的暗号！"
          echo "触发事件类型: ${{ github.event.action }}"
          echo "触发时间: $(date)"
